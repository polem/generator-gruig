{
  "name": "<%= _.camelize(apptitle) %>",
  "version": "0.0.0",
  "dependencies": {
    "jquery": "~2.0.3",<% if (css_preprocessor_use && css_preprocessor == 'compass') { %>
    "bootstrap-sass": "~3.0.0",<% } else { %>
    "bootstrap": "~3.0.0",<% } %>
    "modernizr": "~2.6.2"
  },
  "devDependencies": {
  }
}

